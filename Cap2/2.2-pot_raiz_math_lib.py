from math import pow
from math import sqrt


# Potenciação usando operadores
x = 3
y = 5

potencia = pow(x, y)
print(f'{potencia}({type(potencia)})')

# Radiciação usando operadores
z = 81

raiz_quadrada = sqrt(z)
print(f'{raiz_quadrada}({type(raiz_quadrada)})')
